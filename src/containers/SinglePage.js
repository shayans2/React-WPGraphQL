import React from "react";
import SinglePage from "../components/SinglePage";

const SinglePageContainer = ({ match }) => {
  return <SinglePage match={match} />;
};

export default SinglePageContainer;
