import React from "react";
import SinglePost from "../components/SinglePost";

const SinglePostContainer = ({ match }) => {
  return <SinglePost match={match} />;
};

export default SinglePostContainer;
